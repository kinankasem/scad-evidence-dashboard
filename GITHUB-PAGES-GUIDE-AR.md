# نشر لوحة SCAD على GitHub Pages

## ما الذي سترفعه؟

المجلد `docs` هو النسخة الجاهزة للنشر. يحتوي على ملفات HTML وCSS وJavaScript، الشعارين، ملفي Excel الأصليين، ونسخة بيانات أولية مستخرجة منهما.

## خطوات النشر

1. أنشئ مستودعًا جديدًا في GitHub، وليكن اسمه `scad-evidence-dashboard`.
2. ارفع كامل محتويات مجلد المشروع `scad-dashboard` إلى المستودع، وتأكد من وجود مجلد `docs` في المستوى الرئيسي.
3. في GitHub افتح **Settings → Pages**.
4. تحت **Build and deployment** اختر **Deploy from a branch**.
5. اختر الفرع **main** والمجلد **/docs**، ثم اضغط **Save**.
6. خلال دقائق سيظهر رابط بالشكل التالي:

   `https://YOUR-USERNAME.github.io/scad-evidence-dashboard/`

يمكن لأي شخص يملك الرابط فتح اللوحة. بدون إعداد قاعدة المشاركة، تكون التعديلات محفوظة داخل متصفح كل مستخدم على حدة، ويمكن تنزيلها كملفات Excel محدثة.

## تفعيل التعديل المشترك بين كل الزوار

GitHub Pages موقع ثابت ولا يستطيع الكتابة داخل ملفات Excel الموجودة في المستودع. لتصبح التعديلات مشتركة بين الجميع:

1. أنشئ ملف Google Sheets فارغًا في Google Drive.
2. من داخله افتح **Extensions → Apps Script**.
3. انسخ محتوى الملف `apps-script/Code.gs` والصقه في المحرر، ثم احفظ.
4. اختر **Deploy → New deployment → Web app**.
5. اجعل **Execute as** = `Me`، و**Who has access** = `Anyone`.
6. وافق على الصلاحيات وانسخ رابط Web App المنتهي بـ `/exec`.
7. افتح اللوحة، ثم **Data & Settings**، والصق الرابط في **Shared Data Service URL** واضغط **Save & Enable**.
8. بعد نجاح المزامنة، تصبح الإضافات والتعديلات مشتركة لكل من يملك رابط اللوحة.

> تنبيه: اختيار `Anyone` مقصود لتحقيق طلب السماح لأي شخص معه الرابط بالتعديل. هذا يعني أن أي شخص يصل للرابط يستطيع تغيير أو حذف السجلات. استخدم وصولًا مقيدًا إذا كانت البيانات حساسة.

## تحديث بيانات Excel الأساسية

- من داخل اللوحة استخدم **Import from Excel** لاستيراد ملف أو ملفين محدثين.
- استخدم **Export Updated Copies** لتنزيل ملفي Excel بعد التعديل.
- لتغيير البيانات التي يراها الزوار عند أول فتح للموقع، استبدل الملفين داخل `docs/data` ثم أعد توليد `seed-data.js`، أو استورد الملفات ثم فعّل قاعدة المشاركة.

## ملاحظات مهمة

- لا تحتاج GitHub Pages إلى خادم أو قاعدة بيانات عند استخدام الوضع المحلي.
- الشعاران موجودان داخل `docs/assets`.
- لا تضع كلمات مرور أو مفاتيح سرية داخل `config.js` أو أي ملف مرفوع إلى GitHub.
